---
name: ux-review-fix
description: 'MES 产品交互/UX 系统性 review 的"修复执行"助手：根据 UX review 发现的问题进行代码修复，更新 `00_status.md` 的 Commits 列和问题状态。严格禁止修改 `.claude/skills/**` 与 ux_review 的 meta 模板。'
---

# UX Review Fix（问题修复）

## Goal

把 UX review 发现的问题**闭环**：
- 根据问题证据链定位代码
- 进行最小必要修复
- 更新 `00_status.md` 的 Commits 列（填入 fix commit）
- 更新问题状态为 `closed`

### 优先级（修复顺序）

P0（主线阻断）> P1（体验严重）> P2（体验优化）

同等级别时：业务逻辑 > 功能规划 > UX > UI

---

## Non-Negotiables（硬约束）

### ✅ 允许修改

- `apps/web/src/**`（前端代码）
- `apps/server/src/**`（后端代码）
- `packages/**`（共享包）
- `domain_docs/mes/ux_review/00_status.md`（更新 Commits 列和 P0 表格状态）
- `domain_docs/mes/ux_review/99_high_risk_findings.md`（更新问题状态为 closed）

### 🚫 禁止修改

- `.claude/skills/**`
- `domain_docs/mes/ux_review/00_review_method.md`
- `domain_docs/mes/ux_review/00_interaction_matrix_template.md`
- `domain_docs/mes/ux_review/00_review_backlog.md`
- `domain_docs/mes/ux_review/roundN_*.md`（已产出的 review 文档不改）

> 若用户要求改方法/模板/Backlog：停止执行，改用 `ux-review-meta`。
> 若用户要求新增 review 轮次：停止执行，改用 `ux-review-exec`。

---

## Inputs（必须明确）

### 默认行为（无参数调用）

当用户只输入 `/ux-review-fix` 而没有指定问题时：
1. **不得自动选择问题开始修复**
2. 必须先读取 `00_status.md` 和 `99_high_risk_findings.md`
3. 列出所有 `open` 状态的问题
4. 使用 `AskUserQuestion` 让用户选择

### 指定问题调用

用户可以在调用时提供以下之一：

1. **问题来源**：
   - 从 `99_high_risk_findings.md` 选择 P0 问题（推荐）
   - 从 `roundN_*.md` 的问题清单选择具体条目
   - 直接描述问题（需要先定位到对应 review 条目）

2. **修复范围（可选，默认最小修复）**：
   - `minimal`：只修复指定问题，不做额外重构
   - `batch`：批量修复同一轮次的所有 P0/P1

---

## Workflow（执行步骤）

### Step 0) 确认修复目标（必须先执行）

> ⚠️ **硬性要求**：如果用户没有在调用时明确指定要修复的问题，**必须先读取状态文件并使用 `AskUserQuestion` 让用户选择**，不得自动选择问题开始修复。

1. 读取 `domain_docs/mes/ux_review/00_status.md` 和 `99_high_risk_findings.md`
2. 列出所有 `status: open` 的问题
3. 使用 `AskUserQuestion` 工具让用户选择要修复的问题
4. 等待用户确认后再进入 Step 1

**示例问法**：
```
当前待修复的 P0 问题：
1. OQC_REQUIRED 误判 (Round 1)
2. /mes 入口无权限 (Round 2)
...

你想修复哪个问题？
```

**跳过条件**：仅当用户在调用时已明确指定问题（如 `修复 P0：OQC_REQUIRED`）时，可直接进入 Step 1。

### Step 1) 读取问题详情

1. 打开 `domain_docs/mes/ux_review/00_status.md`，定位问题所属轮次
2. 读取对应 `roundN_*.md`，找到问题的：
   - 复现步骤 / 触发条件
   - 期望 vs 现状
   - 证据链（代码路径）
   - 责任归属（前端/后端/文档/产品）
3. 如果是 P0，同时读取 `99_high_risk_findings.md` 获取影响描述

> 规则：每读 2 个证据文件，就把理解写进 `.scratch/` 草稿（不要只在聊天里）。

### Step 2) 定位代码

根据证据链中的文件路径，读取相关代码：
- 前端：`apps/web/src/routes/**`、`apps/web/src/hooks/**`、`apps/web/src/components/**`
- 后端：`apps/server/src/modules/**`
- 权限：`domain_docs/mes/permission_audit.md`

确认问题根因后再动手，避免"修了表象没修根因"。

### Step 3) 实施修复

遵循项目编码规范（`agent_docs/01_core/coding_standards.md`）：
- 最小必要修改，不做"顺便重构"
- 不引入新的安全漏洞（OWASP Top 10）
- 如涉及权限变更，同步更新 `permission_audit.md`

修复类型参考：
| 问题类型 | 典型修复 |
|---------|---------|
| 业务逻辑（状态/门禁） | 后端 service 逻辑 + 前端状态判断 |
| 功能规划（入口/IA） | 路由/权限 gate / 重定向逻辑 |
| UX（反馈/空态） | Toast/提示文案 / 错误处理 |
| UI（一致性） | 组件样式 / 标题层级 |

### Step 4) 验证修复

1. `bun run lint` + `bun run check-types` 通过
2. 如有相关测试，确保测试通过
3. 手动验证复现步骤不再出现问题

### Step 5) 提交代码

使用规范的 commit message：
```
fix(mes): <简要描述>

- 修复 <问题摘要>
- 证据: <roundN 文件路径>
- Closes UX-P0: <问题标题>
```

### Step 6) 更新状态文档

1. **更新 `00_status.md` 轮次表**：
   - 在对应轮次的 `Commits (review → fix/close)` 列填入 fix commit hash
   - 格式：`review_hash` → `fix_hash`（例如 `6e4d664` → `abc1234`）

2. **更新 `00_status.md` P0 表格**：
   - 将问题状态从 `open` 改为 `closed`
   - 更新 `Updated` 日期

3. **更新 `99_high_risk_findings.md`**（如果是 P0）：
   - 将问题状态从 `open` 改为 `closed`
   - 更新 `Updated` 日期

4. **更新 `roundN_*.md` 问题清单**（可选）：
   - 将 `[ ]` 改为 `[x]`

---

## Output Standard（验收标准）

一次修复合格的最低标准：
- 代码修改已提交（有明确的 fix commit）
- `00_status.md` 的 Commits 列已更新（`→ fix_hash`）
- 如果是 P0，`00_status.md` 和 `99_high_risk_findings.md` 的状态都改为 `closed`
- `bun run lint` + `bun run check-types` 通过

---

## 示例提示词

### 修复单个 P0
```
/ux-review-fix

修复 P0：OQC_REQUIRED 误判为失败
来源：round1_core_execution.md
```

### 批量修复一个轮次
```
/ux-review-fix batch

修复 Round 1 的所有 P0/P1 问题
```

### 修复特定问题
```
/ux-review-fix

问题：SN resolve 失败静默处理
来源：round7_execution_ux.md，问题清单 5.3 第一条
范围：minimal
```

---

## 与其他技能的协作

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  ux-review-meta │ ──▶ │  ux-review-exec │ ──▶ │  ux-review-fix  │
│  (方法/模板)     │     │  (轮次执行)      │     │  (问题修复)      │
└─────────────────┘     └─────────────────┘     └─────────────────┘
        │                       │                       │
        ▼                       ▼                       ▼
   00_review_method.md     roundN_*.md            代码修改
   00_review_backlog.md    00_status.md           00_status.md (fix commit)
   round1_template.md      99_high_risk_findings  问题状态 → closed
```

- **meta**：定义规则，不执行
- **exec**：执行 review，不修复
- **fix**：执行修复，不改规则/review 产出
