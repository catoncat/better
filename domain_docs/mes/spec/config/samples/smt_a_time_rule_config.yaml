version: 1
sample: SMT-A
rules:
  - ruleCode: WASH_4H
    scopeType: ROUTING
    scopeRef: SMT-BOT-标准路由
    enabled: true
    startEvent: TRACK_OUT
    endEvent: TRACK_IN
    maxDurationHours: 4
    alertLevel: ALERT
    waivePermission: time_rule:override
    notes: "Route must include wash step; alert + waive only"
  - ruleCode: SOLDER_PASTE_24H
    scopeType: ROUTING
    scopeRef: SMT-BOT-标准路由
    enabled: true
    startEvent: SOLDER_PASTE_USAGE_CREATE
    endEvent: TRACK_OUT
    maxDurationHours: 24
    alertLevel: ALERT
    waivePermission: time_rule:override
    notes: "Issued to track-out, alert + waive only"
  - ruleCode: SOLDER_PASTE_24H
    scopeType: PRODUCT
    scopeRef: 5223029018
    enabled: true
    startEvent: SOLDER_PASTE_USAGE_CREATE
    endEvent: TRACK_OUT
    maxDurationHours: 16
    alertLevel: ALERT
    waivePermission: time_rule:override
    notes: "Product override: stricter limit"
